// Numbers
let age = 21;
let salary = 350000;
let gpa = 8.5;

// Strings (text)
let name = "Ayushi";
let city = "Ahmedabad";
let stack = "Full Stack";

// Booleans (true or false only)
let isEmployed = true;
let knowsReact = false;
let isSerious = true;

// Check what TYPE each variable is
console.log(typeof age);
console.log(typeof name);
console.log(typeof isEmployed);

// Combining them
console.log(name + " lives in " + city);
console.log("Employed: " + isEmployed);
console.log("Knows React: " + knowsReact + " (not yet, but will soon)");

// What happens when you add a number and a string?
let result1 = age + salary;
let result2 = age + name;

console.log(result1);
console.log(result2);